# Track A (issue #3) — corrected HSSM OU fits to the constant-bound RNN behaviour

Branch `track-a-hssm-ou-corrected` (from `ou-round2-base`). Code: `track_a/`. Tables and figures:
`output/track_a/`. Netcdfs stay on Oscar under the same path.

**Sign convention, stated once and repeated in every table: ssm-simulators / HSSM implement
`dx = (v − g·x) dt + dW`, so `g > 0` is LEAKY (recency) and `g < 0` is UNSTABLE / attractive (primacy).**
The landscape's drift slope uses the opposite sign (negative = restoring = leaky). Boundaries are at −a
and +a; z is the relative start point (0.5 = unbiased).

*(placeholder — filled in below as the fits land)*
